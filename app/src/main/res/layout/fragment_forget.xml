<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="100dp"
        android:id="@+id/forget_fragment_iv"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="60dp"
        android:src="@drawable/logoo"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/forget_fragment_tv"
        app:layout_constraintTop_toBottomOf="@id/forget_fragment_iv"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="@string/forget_passwoed"
        android:textSize="20sp"
        android:layout_marginTop="16dp"
        android:textColor="@color/black"

        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="@dimen/dim_40dp"
        app:layout_constraintTop_toBottomOf="@id/forget_fragment_tv"
        android:layout_margin="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/rectangle"
        android:hint="phone"
        android:paddingStart="@dimen/dim_8dp"
        android:textColorHint="@color/black"
        android:textStyle="bold"
        android:inputType="number"
        android:textSize="15sp"
        android:id="@+id/forget_fragment_et"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/forget_fragment_et"
        android:layout_marginEnd="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="32dp"
        android:background="@color/green"
        android:textSize="20sp"
        android:text="@string/send"
        android:id="@+id/forget_fragment_btn"
        android:textColor="@color/white"




        />











</android.support.constraint.ConstraintLayout>